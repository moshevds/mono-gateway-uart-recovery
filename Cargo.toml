[workspace]
members = [
    "protocol",
    "host",
    "stub",
]
resolver = "2"

[workspace.package]
edition = "2021"
license = "MIT"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = "symbols"
